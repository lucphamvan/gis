import { Provider } from "@/components/ui/provider";
import GisModule from "@/modules/GisModule";

const App = () => {
  return (
    <Provider>
      <GisModule />
    </Provider>
  );
};

export default App;
